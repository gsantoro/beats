apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: configmap-role
rules:
  - apiGroups:
      - ""
    resourceNames:
      - kubeadm-config
    resources:
      - configmaps
    verbs:
      - get
